plugins {
    id("application")
}

description = "Crypto exchange market data ingestion"

dependencies {
    implementation(project(":common"))
    implementation("org.apache.kafka:kafka-clients:4.0.0")
}